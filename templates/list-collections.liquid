<div class="container">
  <header class="section-header text-center">
    <h1>{{ page_title }}</h1>
  </header>

  <div class="row">
    {% for collection in collections %}
      <div class="col-xl-20 col-md-30">
        {% include 'collection-grid-item' %}
      </div>
    {% endfor %}
  </div>
</div>

